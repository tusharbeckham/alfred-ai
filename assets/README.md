# assets

Put the demo recording here as **`demo.gif`** (exact name, lowercase).
The main README references it at `assets/demo.gif` and links it to the live demo.

Tip: keep it under ~10 MB so GitHub renders it inline. A short loop (10-20s) at
~1000px wide looks best.
